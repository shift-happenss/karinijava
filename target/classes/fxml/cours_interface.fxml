<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="controllers.CoursController"
      spacing="20"
      style="-fx-background-color: linear-gradient(to bottom, #1d2671, #dfc51e); -fx-padding: 30;">

   <HBox spacing="20" VBox.vgrow="ALWAYS">

      <!-- Sidebar -->
      <fx:include source="sidebar.fxml"/>

      <!-- Contenu principal -->
      <VBox spacing="25"
            alignment="TOP_CENTER"
            style="-fx-padding: 30;"
            HBox.hgrow="ALWAYS">

         <Label text="📚 Liste des Cours"
                style="-fx-font-size: 30px; -fx-font-weight: bold; -fx-text-fill: white;" />

         <!-- Boutons améliorés -->
         <HBox spacing="20">
            <Button text="📚 Voir les examens"
                    onAction="#openExamenInterface"
                    style="-fx-background-color: #2980b9; -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold;
                           -fx-padding: 12 25; -fx-background-radius: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 5, 0, 0, 2);" />

            <Button text="➕ Ajouter un cours"
                    onAction="#openAddCoursPopup"
                    style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold;
                           -fx-padding: 12 25; -fx-background-radius: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.3), 5, 0, 0, 2);" />
         </HBox>

         <!-- Liste des cours -->
         <ScrollPane hbarPolicy="NEVER" vbarPolicy="ALWAYS"
                     style="-fx-background-color: transparent; -fx-border-width: 0;">
            <GridPane fx:id="coursContainer"
                      hgap="20" vgap="20"
                      alignment="TOP_CENTER"
                      style="-fx-padding: 20;">
            </GridPane>
         </ScrollPane>

      </VBox>
   </HBox>

</VBox>
